# coding: utf-8

# -----------------------------------------------------------------------------
# Local configuration template.
# Copy this file to `config.py` and fill in the real values.
# `config.py` is git-ignored so your token / local path will not be committed.
#
# ローカル設定テンプレート。
# このファイルを `config.py` にコピーし、実際の値を記入してください。
# `config.py` は .gitignore 対象なので、トークンやローカルパスは Git にコミット
# されません。
# -----------------------------------------------------------------------------

# PerfDog token applied from the PerfDog console.
# PerfDog コンソールで申請した認証トークン。
# https://perfdog.qq.com/
SERVICE_TOKEN = '-'

# Absolute path to the PerfDogService binary on this machine.
# 本マシン上の PerfDogService バイナリの絶対パス。
#   macOS  ex: /Users/<you>/Downloads/PerfDogService(...)/PerfDogService
#   Windows ex: C:\\Tools\\PerfDogService\\PerfDogService.exe
SERVICE_PATH = '-'
